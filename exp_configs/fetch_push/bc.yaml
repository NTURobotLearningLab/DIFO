project: difo
program: scripts.train_imitation
method: grid
name: FetchPush-BC
command:
  - ${env}
  - python3
  - -m
  - ${program}
  - bc
  - with
  - bc
  - fetch_push
  - ${args_no_hyphens}
  - --force

parameters:
  seed:
    value: 0

project: difo
program: scripts.train_imitation
method: grid
name: FetchPush-BCO
command:
  - ${env}
  - python3
  - -m
  - ${program}
  - bco
  - with
  - bco
  - fetch_push
  - ${args_no_hyphens}
  - --force

parameters:
  seed:
    value: 0
